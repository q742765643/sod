<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.piesat.monitor.dao.es.rabbitmq.RabbitMqMonitorMapper" >
    <resultMap id="BaseResultMap" type="com.piesat.monitor.entity.rabbitmq.RabbitMqMonitor">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <result column="@timestamp" property="timestamp" />
        <result column="host.name" property="hostname" />
        <result column="event.dataset"  property="dataset" />
        <result column="service.address"  property="address" />
        <result column="rabbitmq.queue.name"  property="queueName" />
        <result column="rabbitmq.queue.messages.total.count"  property="totalCount" />
        <result column="rabbitmq.queue.messages.ready.count"  property="readyCount" />
        <result column="rabbitmq.queue.messages.unacknowledged.count"  property="unacknowledgedCount" />

        <result column="rabbitmq.queue.messages.ready.details.rate"  property="readyDetailsRate" />
        <result column="rabbitmq.queue.messages.total.details.rate"  property="totalDetailsRate" />
        <result column="rabbitmq.queue.messages.unacknowledged.details.rate"  property="unacknowledgedDetailsRate" />

    </resultMap>
    <sql id="Base_Column_List">
         "@timestamp",
         "host.name",
         "event.dataset",
         "rabbitmq.vhost",
         "rabbitmq.queue.name",
         "rabbitmq.queue.messages.total.count",
         "rabbitmq.queue.messages.ready.count",
         "rabbitmq.queue.messages.unacknowledged.count",
         "rabbitmq.queue.messages.ready.details.rate",
         "rabbitmq.queue.messages.total.details.rate",
         "rabbitmq.queue.messages.unacknowledged.details.rate"

    </sql>

    <select id="list" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from
        "metricbeat-*"
        where 1=1
        <if test="hostname != null and hostname != ''">
            AND "host.name"= #{hostname}
        </if>
        <if test="endTime != null ">
            AND "@timestamp" &lt;= #{endTime}
        </if>
        <if test="startTime != null ">
            AND  "@timestamp" &gt;= #{startTime}
        </if>
        <if test="dataset != null and dataset != ''">
            AND  "event.dataset" = #{dataset}
        </if>
        <if test="address != null and address != ''">
            AND  "service.address" like '%${address}%'
        </if>
        <if test="timestamp != null ">
            AND "@timestamp" = #{timestamp}
        </if>
    </select>
    <select id="maxTime" resultType="java.util.Date">
        select
         max("@timestamp")
        from
        "metricbeat-*"
        where 1=1
        <if test="hostname != null and hostname != ''">
            AND "host.name"= #{hostname}
        </if>
        <if test="endTime != null ">
            AND "@timestamp" &lt;= #{endTime}
        </if>
        <if test="startTime != null ">
            AND  "@timestamp" &gt;= #{startTime}
        </if>
        <if test="dataset != null and dataset != ''">
            AND  "event.dataset" = #{dataset}
        </if>
        <if test="address != null and address != ''">
            AND  "service.address" like '%${address}%'
        </if>
    </select>
</mapper>