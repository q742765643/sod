<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.piesat.monitor.dao.es.rabbitmq.RabbitMqConfigMapper" >
    <resultMap id="BaseResultMap" type="com.piesat.monitor.entity.rabbitmq.RabbitMqConfig">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <result column="id" property="id" />
        <result column="createTime" property="createTime" />
        <result column="address"  property="address" />
        <result column="alias"  property="alias" />

    </resultMap>
    <sql id="Base_Column_List">
         id,createTime,address,alias

    </sql>

    <select id="list" resultMap="BaseResultMap">
        select
        *
        from
        rabbitmq_config
        where 1=1
        <if test="address != null and address != ''">
            AND address LIKE '%${address}%'
        </if>
        <if test="alias != null and alias != ''">
            AND alias LIKE '%${alias}%'
        </if>
        ORDER BY createTime desc
    </select>
    <select id="selectAlias"  resultType="java.lang.String">
        select
        alias
        from
        rabbitmq_config
        GROUP by alias
    </select>
</mapper>