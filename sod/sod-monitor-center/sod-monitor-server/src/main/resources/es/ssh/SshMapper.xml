<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.piesat.monitor.dao.es.ssh.SshMapper" >
    <resultMap id="BaseResultMap" type="com.piesat.monitor.entity.ssh.SshEntity">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <result column="id" property="id" />
        <result column="ip" property="ip" />
        <result column="port"  property="port" />
        <result column="userName"  property="userName" />
        <result column="passWord"  property="passWord" />
        <result column="createTime"  property="createTime"  />
        <result column="hostName"  property="hostName" />
        <result column="alias"  property="alias" />

    </resultMap>
    <sql id="Base_Column_List">
         id,
         ip,
         userName,
         port,
         passWord,
         createTime,
         hostName
    </sql>

    <select id="list" resultMap="BaseResultMap">
        select
        *
        from
        "ssh"
        where 1=1
        <if test="ip != null and ip != ''">
            AND ip like  '%${ip}%'
        </if>
        <if test="hostName!= null and hostName != ''">
            AND hostName like '%${hostName}%'
        </if>
        <if test="alias != null and alias != ''">
            AND alias like '%${alias}%'
        </if>

         ORDER BY createTime desc
    </select>
    <select id="selectAlias"  resultType="java.lang.String">
        select
        alias
        from
        ssh
        GROUP by alias
    </select>

</mapper>