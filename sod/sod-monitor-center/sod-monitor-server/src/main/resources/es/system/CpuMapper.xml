<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.piesat.monitor.dao.es.system.CpuMapper" >
    <resultMap id="BaseResultMap" type="com.piesat.monitor.entity.system.CpuEntity">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <result column="@timestamp" property="timestamp" />
        <result column="host.name" property="hostname" />
        <result column="event.dataset"  property="dataset" />
        <result column="system.cpu.cores"  property="cpuCores" />
        <result column="system.cpu.user.pct"  property="cpuUserPct" />
        <result column="system.cpu.idle.pct"  property="cpuIdlePct" />
        <result column="system.cpu.iowait.pct"  property="cpuIowaitPct" />
        <result column="system.cpu.irq.pct"  property="cpuIrqPct" />
        <result column="system.cpu.softirq.pct"  property="cpuSoftirqPct" />
        <result column="system.cpu.nice.pct"  property="cpuNicePct" />
        <result column="system.cpu.system.pct"  property="cpuSystemPct" />
        <result column="system.cpu.total.pct"  property="totalPct" />


    </resultMap>
    <sql id="Base_Column_List">
         "@timestamp",
         "host.name",
         "event.dataset",
         "system.cpu.cores",
         "system.cpu.user.pct",
         "system.cpu.idle.pct",
         "system.cpu.iowait.pct",
         "system.cpu.irq.pct",
         "system.cpu.softirq.pct",
         "system.cpu.nice.pct",
         "system.cpu.system.pct",
         "system.cpu.total.pct"/"system.cpu.cores"  "system.cpu.total.pct"
    </sql>

    <select id="list" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from
        "metricbeat-*"
        where 1=1
        <if test="hostname != null and hostname != ''">
            AND "host.name"= #{hostname}
        </if>
        <if test="endTime != null ">
            AND "@timestamp" &lt;= #{endTime}
        </if>
        <if test="startTime != null ">
            AND  "@timestamp" &gt;= #{startTime}
        </if>
        <if test="dataset != null and dataset != ''">
            AND  "event.dataset" = #{dataset}
        </if>
         ORDER BY "@timestamp" desc limit 1000
    </select>

</mapper>