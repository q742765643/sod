<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.piesat.monitor.dao.es.system.MemoryMapper" >
    <resultMap id="BaseResultMap" type="com.piesat.monitor.entity.system.MemoryEntity">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <result column="@timestamp" property="timestamp" />
        <result column="host.name" property="hostname" />
        <result column="event.dataset"  property="dataset" />
        <result column="system.memory.actual.free"  property="free" />
        <result column="system.memory.actual.used.bytes"  property="usedBytes" />
        <result column="system.memory.actual.used.pct"  property="usedPct" />


    </resultMap>
    <sql id="Base_Column_List">
         "@timestamp",
         "host.name",
         "event.dataset",
         "system.memory.actual.free",
         "system.memory.actual.used.bytes",
         "system.memory.actual.used.pct"
    </sql>

    <select id="list" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from
        "metricbeat-*"
        where 1=1
        <if test="hostname != null and hostname != ''">
            AND "host.name"= #{hostname}
        </if>
        <if test="endTime != null ">
            AND "@timestamp" &lt;= #{endTime}
        </if>
        <if test="startTime != null ">
            AND  "@timestamp" &gt;= #{startTime}
        </if>
        <if test="dataset != null and dataset != ''">
            AND  "event.dataset" = #{dataset}
        </if>
        ORDER BY "@timestamp" desc limit 1000

    </select>

</mapper>