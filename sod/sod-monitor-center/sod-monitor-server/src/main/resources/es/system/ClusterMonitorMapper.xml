<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.piesat.monitor.dao.es.system.ClusterMonitorMapper" >
    <resultMap id="BaseResultMap" type="com.piesat.monitor.entity.system.ClusterMonitor">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <result column="ip" property="ip" />
        <result column="totalCpuPct" property="totalCpuPct" />
        <result column="usedPct"  property="usedPct" />
        <result column="filePct"  property="filePct" />
        <result column="netWork"  property="netWork" />
        <result column="process"  property="process" />

    </resultMap>
    <sql id="Base_Column_List">
         "host.name" ip,
         avg("system.cpu.total.pct")/avg("system.cpu.cores") totalCpuPct,
         avg("system.memory.actual.used.pct") usedPct,
         avg("system.filesystem.used.pct") filePct,
         max("system.network.in.packets") netWork
    </sql>

    <select id="list" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from
        "metricbeat-*"
        where 1=1 and "event.module"='system'
        <if test="ips != null">
            AND  "host.name" IN
            <foreach collection="ips" item="item" index="index" open="(" close=")" separator=",">
                  #{item}
            </foreach>
        </if>
        <if test="endTime != null ">
            AND "@timestamp" &lt;= #{endTime}
        </if>
        <if test="startTime != null ">
            AND  "@timestamp" &gt;= #{startTime}
        </if>
         GROUP BY "host.name" ORDER BY "host.name" desc
    </select>

</mapper>