<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.piesat.monitor.dao.es.system.NetWorkMapper" >
    <resultMap id="BaseResultMap" type="com.piesat.monitor.entity.system.NetWorkEntity">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <result column="@timestamp" property="timestamp" />
        <result column="host.name" property="hostname" />
        <result column="event.dataset"  property="dataset" />
        <result column="system.network.in.packets"  property="inPackets" />
        <result column="system.network.in.bytes"  property="inBytes" />
        <result column="system.network.out.packets"  property="outPackets" />
        <result column="system.network.out.bytes"  property="outBytes" />
        <result column="sumInBytes"  property="sumInBytes" />
        <result column="sumOutBytes"  property="sumOutBytes" />



    </resultMap>
    <sql id="Base_Column_List">
         "@timestamp",
         max("system.network.in.packets") "system.network.in.packets",
         max("system.network.in.bytes") "system.network.in.bytes",
         max("system.network.out.packets") "system.network.out.packets",
         max("system.network.out.bytes") "system.network.out.bytes",
         sum("system.network.in.bytes") sumInBytes,
         sum("system.network.out.bytes") sumOutBytes

    </sql>

    <select id="list" resultMap="BaseResultMap">

        select
        <include refid="Base_Column_List" />
        from
        "metricbeat-*"
        where "event.dataset" = #{dataset} and "system.network.name" not like 'l%'
        <if test="hostname != null and hostname != ''">
            AND "host.name"= #{hostname}
        </if>
        <if test="endTime != null ">
            AND "@timestamp" &lt;= #{endTime}
        </if>
        <if test="startTime != null ">
            AND  "@timestamp" &gt;= #{startTime}
        </if>
        GROUP BY "@timestamp"
        ORDER BY "@timestamp" desc limit 1000

    </select>

</mapper>