<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.piesat.monitor.dao.es.system.ProcessMapper" >
    <resultMap id="BaseResultMap" type="com.piesat.monitor.entity.system.ProcessEntity">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <result column="@timestamp" property="timestamp" />
        <result column="host.name" property="hostname" />
        <result column="event.dataset"  property="dataset" />
        <result column="system.process.cmdline"  property="cmdline" />
        <result column="process.args"  property="cwd" />
        <result column="process.name"  property="name" />
        <result column="process.pgid"  property="pgid" />
        <result column="process.pid"  property="pid" />
        <result column="process.ppid"  property="ppid" />
        <result column="system.process.state"  property="state" />
        <result column="user.name"  property="username" />


    </resultMap>
    <sql id="Base_Column_List">
         "@timestamp",
         "host.name",
         "event.dataset",
         "system.process.cmdline",
         "process.args",
         "process.name",
         "process.pgid",
         "process.pid",
         "process.ppid",
         "system.process.state",
         "user.name"

    </sql>

    <select id="list" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from
        "metricbeat-*"
        where 1=1 AND "system.process.cmdline" is NOT  NULL
        <if test="hostname != null and hostname != ''">
            AND "host.name"= #{hostname}
        </if>
        <if test="endTime != null ">
            AND "@timestamp" &lt;= #{endTime}
        </if>
        <if test="startTime != null ">
            AND  "@timestamp" &gt;= #{startTime}
        </if>
        <if test="dataset != null and dataset != ''">
            AND  "event.dataset" = #{dataset}
        </if>
        <if test="timestamp != null ">
            AND "@timestamp" = #{timestamp}
        </if>
    </select>
    <select id="maxTime" resultType="java.util.Date">
        select
         max("@timestamp")
        from
        "metricbeat-*"
        where 1=1
        <if test="hostname != null and hostname != ''">
            AND "host.name"= #{hostname}
        </if>
        <if test="endTime != null ">
            AND "@timestamp" &lt;= #{endTime}
        </if>
        <if test="startTime != null ">
            AND  "@timestamp" &gt;= #{startTime}
        </if>
        <if test="dataset != null and dataset != ''">
            AND  "event.dataset" = #{dataset}
        </if>
    </select>
</mapper>