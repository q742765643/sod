<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.piesat.monitor.dao.es.system.FileMapper" >
    <resultMap id="BaseResultMap" type="com.piesat.monitor.entity.system.FileEntity">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <result column="@timestamp" property="timestamp" />
        <result column="host.name" property="hostname" />
        <result column="event.dataset"  property="dataset" />
        <result column="system.filesystem.device_name"  property="deviceName" />
        <result column="system.filesystem.type"  property="ftype" />
        <result column="system.filesystem.mount_point"  property="mountPoint" />
        <result column="system.filesystem.free"  property="free" />
        <result column="system.filesystem.total"  property="total" />
        <result column="system.filesystem.used.bytes"  property="usedBytes" />
        <result column="system.filesystem.used.pct"  property="usedPct" />


    </resultMap>
    <sql id="Base_Column_List">
         "@timestamp",
         "host.name",
         "event.dataset",
         "system.filesystem.device_name",
         "system.filesystem.type",
         "system.filesystem.mount_point",
         "system.filesystem.free",
         "system.filesystem.total",
         "system.filesystem.used.bytes",
         "system.filesystem.used.pct"

    </sql>

    <select id="list" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from
        "metricbeat-*"
        where 1=1
        <if test="hostname != null and hostname != ''">
            AND "host.name"= #{hostname}
        </if>
        <if test="endTime != null ">
            AND "@timestamp" &lt;= #{endTime}
        </if>
        <if test="startTime != null ">
            AND  "@timestamp" &gt;= #{startTime}
        </if>
        <if test="dataset != null and dataset != ''">
            AND  "event.dataset" = #{dataset}
        </if>
        <if test="timestamp != null ">
            AND "@timestamp" = #{timestamp}
        </if>
        ORDER BY "@timestamp" desc limit 1000

    </select>
    <select id="maxTime" resultType="java.util.Date">
        select
         max("@timestamp")
        from
        "metricbeat-*"
        where 1=1
        <if test="hostname != null and hostname != ''">
            AND "host.name"= #{hostname}
        </if>
        <if test="endTime != null ">
            AND "@timestamp" &lt;= #{endTime}
        </if>
        <if test="startTime != null ">
            AND  "@timestamp" &gt;= #{startTime}
        </if>
        <if test="dataset != null and dataset != ''">
            AND  "event.dataset" = #{dataset}
        </if>
    </select>
</mapper>