<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.piesat.monitor.dao.es.kafka.KafkaMonitorMapper" >
    <resultMap id="BaseResultMap" type="com.piesat.monitor.entity.kafka.KafkaMonitor">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <result column="@timestamp" property="timestamp" />
        <result column="host.name" property="hostname" />
        <result column="event.dataset"  property="dataset" />
        <result column="kafka.consumergroup.broker.address"  property="address" />
        <result column="kafka.consumergroup.id"  property="groupId" />
        <result column="kafka.consumergroup.topic"  property="topic" />
        <result column="kafka.consumergroup.consumer_lag"  property="consumerLag" />


    </resultMap>
    <sql id="Base_Column_List">
         "kafka.consumergroup.topic",
          "kafka.consumergroup.broker.address",
           "kafka.consumergroup.id",
         sum("kafka.consumergroup.consumer_lag") "kafka.consumergroup.consumer_lag"
    </sql>

    <select id="list" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from
        "metricbeat-*"
        where 1=1
        <if test="endTime != null ">
            AND "@timestamp" &lt;= #{endTime}
        </if>
        <if test="startTime != null ">
            AND  "@timestamp" &gt;= #{startTime}
        </if>
        <if test="dataset != null and dataset != ''">
            AND  "event.dataset" = #{dataset}
        </if>
        <if test="timestamp != null ">
            AND "@timestamp" = #{timestamp}
        </if>
        <if test="groupId != null and groupId != ''">
            AND  "kafka.consumergroup.id" = #{groupId}
        </if>
        <if test="addresss != null ">
            AND
            <foreach collection="addresss" item="item" index="index" open="(" close=")" separator=" or ">
                "kafka.consumergroup.broker.address" like '%${item}'
            </foreach>
        </if>
        <if test="groupIds != null">
            AND
            <foreach collection="groupIds" item="item" index="index" open="(" close=")" separator=" or ">
                  "kafka.consumergroup.id" LIKE '%${item}%'
            </foreach>
        </if>
        group by "kafka.consumergroup.topic","kafka.consumergroup.broker.address",
        "kafka.consumergroup.id"
    </select>
    <select id="maxTime" resultType="java.util.Date">
        select
        max("@timestamp")
        from
        "metricbeat-*"
        where 1=1
        <if test="endTime != null ">
            AND "@timestamp" &lt;= #{endTime}
        </if>
        <if test="startTime != null ">
            AND  "@timestamp" &gt;= #{startTime}
        </if>
        <if test="dataset != null and dataset != ''">
            AND  "event.dataset" = #{dataset}
        </if>
        <if test="groupId != null and groupId != ''">
            AND  "kafka.consumergroup.id" = #{groupId}
        </if>
        <if test="addresss != null">
            AND
            <foreach collection="addresss" item="item" index="index" open="(" close=")" separator=" or ">
                "kafka.consumergroup.broker.address" like '%${item}'
            </foreach>
        </if>
    </select>
</mapper>