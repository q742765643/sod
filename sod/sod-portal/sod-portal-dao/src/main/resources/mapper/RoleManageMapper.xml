<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.piesat.portal.mapper.RoleManageMapper">
    <select id="selectRoleListByUserId" parameterType="String" resultType="String">
        select r.id
        from T_SOD_PORTAL_ROLE r
                 left join T_SOD_PORTAL_USER_ROLE ur on ur.role_id = r.id
                 left join T_SOD_PORTAL_USER u on u.id = ur.user_id
        where u.id = #{userId}
    </select>
</mapper>